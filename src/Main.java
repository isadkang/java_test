class Main {
    public static void main(String[] args) {
        // ternary operator
        boolean isShow = true;
        String result;
        result = (isShow == true) ? "Hidup" : "Mati";
        System.out.println(result);
    }
}